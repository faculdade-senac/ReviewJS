/* eslint-disable implicit-arrow-linebreak */
/* eslint-disable no-unused-vars */
const array = [1, 2, 3, 4, 5, 6, 7];

const result = array.map((value) => value * 2);

const result2 = array.map((value) => value * 2);

const result3 = array.map((value) =>
// outros códigos
  value * 2);
